{
  "name": "my-project",
  "scripts": {
    "serve": "mini-website-packer serve --conf ./mwp.confing.js --lang zh",
    "build": "mini-website-packer build --conf ./mwp.confing.js --mini --babel",
    "build-readonly": "mini-website-packer build --conf ./mwp.confing.js --mini --babel --readonly"
  },
  "devDependencies": {
    "mini-website-packer": "^1.2.7"
  }
}
