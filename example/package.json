{
  "name": "my-project",
  "scripts": {
    "serve": "mini-website-packer serve --conf ./src/config.json --lang zh",
    "build": "mini-website-packer build --conf ./src/config.json --mini",
    "build-readonly": "mini-website-packer build --conf ./src/config.json --mini --readonly"
  },
  "devDependencies": {
    "mini-website-packer": "^1.0.1"
  }
}
