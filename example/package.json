{
  "name": "my-project",
  "scripts": {
    "serve": "mini-website-packer serve --conf ./src/config.json --lang zh",
    "build": "mini-website-packer build --conf ./src/config.json --mini --babel",
    "build-readonly": "mini-website-packer build --conf ./src/config.json --mini --babel --readonly"
  },
  "devDependencies": {
    "mini-website-packer": "^1.2.0"
  }
}
